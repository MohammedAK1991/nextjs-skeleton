import { useTranslation } from "react-i18next"

/**
 * Nav bar.
 */
function Navigation({ id }: { id: string }) {
    const { t } = useTranslation()

    return (
    <header className="flex items-center p-4 border-b border-surface-light">
     
    </header>
    )
}

export default Navigation